import initialState from '../store/initialState';
// import * as types from '../actions/ActionTypes';

const RegistryReducer = (state=initialState.registeries, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default RegistryReducer