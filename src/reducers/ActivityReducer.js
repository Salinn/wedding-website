import initialState from '../store/initialState';
import * as types from '../actions/ActionTypes';

const ActivityReducer = (state=initialState.activities, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default ActivityReducer