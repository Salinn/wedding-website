import initialState from '../store/initialState';
// import * as types from '../actions/ActionTypes';

const WeddingPhotosReducer = (state = initialState.events, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default WeddingPhotosReducer